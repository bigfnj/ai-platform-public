# solution-plays — to be authored

This collection is intentionally empty. Its subject matter is currently served from
`../smb-segment/`, which carries researched content on solution plays, discovery questions and
objection handling drawn from public Microsoft sources.

This folder exists for **field-earned material** — the plays, questions, objections and proof
points that come from real partner conversations rather than documentation. That content is
authored with the team, not researched.

READMEs are skipped by the ingester, so an empty collection here costs nothing at retrieval
time. Add `*.md` files following the contract in `../README.md` when there is real material.
