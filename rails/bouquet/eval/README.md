# eval artifacts

Vision eval baselines + model-A/B results land here (`--out eval/...json`), committed so
the recall trend and confusion history are tracked with the code.

See `../docs/vision-eval.md` for the runbook.
