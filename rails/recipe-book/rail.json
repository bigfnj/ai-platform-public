{
  "$schema": "../../docs/rail-manifest.schema.json",
  "id": "recipe-book",
  "label": "Recipe Book",
  "description": "A cookbook of roughly 900 recipes and cocktails with generated illustrations, a meal planner that works in real dates, and a pantry and bar you can cook from.",
  "icon": "🍳",
  "package": "recipe_book",
  "federation_name": "recipe_book",
  "css_wrapper": "recipe-book",
  "layout": "src",
  "env_prefix": "RECIPE_BOOK_",
  "ports": {
    "backend": 8830,
    "dev": 5280
  },
  "status_route": "/api/capabilities",
  "model_slots": [
    {
      "slot": "assistant",
      "label": "Culinary assistant",
      "env": "RECIPE_BOOK_ASSISTANT_MODEL",
      "role": "recipe",
      "kind": "chat",
      "admin_panel": true
    },
    {
      "slot": "vision",
      "label": "Recipe-photo reader",
      "env": "RECIPE_BOOK_VISION_MODEL",
      "role": "recipe-vision",
      "kind": "vision",
      "admin_panel": true
    },
    {
      "slot": "icon",
      "label": "Recipe icon images",
      "env": "RECIPE_BOOK_ICON_MODEL",
      "role": "recipe-icon",
      "kind": "image",
      "admin_panel": true
    }
  ],
  "notes": [
    "Moved off dev port 5240 to 5280: it collided with bouquet AND terminal-fun, so two of the three had to move. The second `npm run dev` to start loses, and its /api proxy silently belongs to whichever rail booted first.",
    "The icon slot is an IMAGE backend (flux-schnell) running on the broker's media worker, not an Ollama tag — it never appears in /v1/models, so any future chip must resolve it against media state rather than the model list.",
    "Don't normalise the culinary assistant to @chat or the icon to a generic image role; both are deliberately rail-specific roles.",
    "Bulk icon regeneration needs a raised RECIPE_BOOK_BROKER_TIMEOUT and big chunks (~250) to amortise the model cold-load — see CLAUDE.local.md."
  ]
}
