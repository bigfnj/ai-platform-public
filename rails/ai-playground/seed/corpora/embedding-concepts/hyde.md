# Answer-first retrieval

Questions are worded very differently from the passages that answer them, which weakens direct matching. One trick asks a generative model to draft a plausible answer first, then searches with that draft instead of the raw question. The invented passage looks structurally like the real answers, closing the phrasing gap even though some of its details may be wrong.
