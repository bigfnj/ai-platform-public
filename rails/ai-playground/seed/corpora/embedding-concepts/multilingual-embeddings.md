# Cross-language matching

A cross-lingual encoder places text from many languages into one shared space, so a phrase in English and its translation in Spanish land near each other. That lets a single index serve questions and documents in different tongues without translation. Models tuned only on one language collapse this ability, so language coverage is a first-class selection criterion.
