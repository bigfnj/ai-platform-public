# The embedding leaderboard

A large public suite scores encoders across dozens of tasks, from retrieval to clustering to classification, and reports one aggregate figure per model. It is the usual first stop when shortlisting, but a headline average can mislead for a specific corpus, which is why teams re-test finalists on their own data before committing.
