# Mixing dense and lexical

Vector matching captures meaning but can miss an exact code, name, or rare term. Classic keyword scoring nails those literal hits but misses paraphrase. Blending the two, then fusing their ranks, gives results that are both meaning-aware and precise on literal tokens. The blend weight is tuned to the collection.
